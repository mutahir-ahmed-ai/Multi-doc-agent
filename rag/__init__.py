# This empty file tells Python that rag/ is a Python package.
# Without it, "from rag.document_processor import ..." would fail with ModuleNotFoundError.
# This is the same pattern used in your Research Agent's agent/ and tools/ folders.
